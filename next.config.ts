import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
    /* config options here */
    // images: {
    //     remotePatterns: [
    //         {
    //             protocol: 'https',
    //             hostname: '**.ravelrycache.com', // 모든 ravelry 캐시 서버 허용
    //         },
    //     ],
    // },
};

export default nextConfig;
